version: '3.8'

services:
  certsync-cos:
    build: .
    container_name: certsync-cos
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config/config.yaml:ro
      - ./certs:/app/certs
    environment:
      - TZ=Asia/Shanghai